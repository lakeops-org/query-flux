# QueryFlux MySQL wire listen address (from host: localhost; from Docker network: queryflux)
QUERYFLUX_MYSQL_HOST=127.0.0.1
QUERYFLUX_MYSQL_PORT=3306
# Must match cluster auth for the mysqlWire route (examples/full-stack: root / empty)
QUERYFLUX_MYSQL_USER=root
QUERYFLUX_MYSQL_PASSWORD=
