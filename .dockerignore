# VCS
.git
.github

# Rust
target/
**/*.rs.bk

# Python (host venv; image builds its own)
.venv/
venv/

# Studio: unified image copies queryflux-studio/; keep heavy dirs out of context.
queryflux-studio/node_modules/
queryflux-studio/.next/

# Other UI roots (not used by Dockerfiles)
ui/

# Local / editor
.idea
.vscode
.DS_Store
*.log

# Docker compose stacks (not needed for image build)
docker/*.yml
